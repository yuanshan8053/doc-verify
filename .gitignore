# Fact base — generated output (per product, per user)
# Contains credentials, screenshots, verification results
# Each user generates their own from source code + console
fact-base/

# Chrome runtime profile (contains cookies, login data, sessions)
.playwright/profile/

# Node
node_modules/

# macOS
.DS_Store

# POCs — proof-of-concepts (per product)
# Contains code, screenshots, verification results
# Each user generates their own from source code + console
poc/

# Local examples — not published to the repository
examples/
